import styled from "styled-components";

const headerContainer = styled.div`
  font-family: Impact;
`;
const header_logo = styled.span`
  color: blue;
`;

const header_navbar = styled.button`
  color: blue;
`;

const header_login = styled.button`
  color: green;
`;

const header_signup = styled.button`
  color: red;
`;

const header_basket = styled.button`
  color: blue;
`;

const styles = {
  header_logo,
  header_navbar,
  header_login,
  header_signup,
  header_basket,
  headerContainer,
};

export default styles;
